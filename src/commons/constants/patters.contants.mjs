/* eslint-disable no-useless-escape */
/**
 * contains patters for validations
 */
export const Patterns = {
  email: /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/,
  phoneCode: /^\+\d+$/,
  phoneNumber: /^[6-9]\d{9}$/,
};
